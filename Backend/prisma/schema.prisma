generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int                  @id @default(autoincrement())
  fullName  String
  email     String               @unique
  password  String
  createdAt DateTime             @default(now())
  otp       String?
  otpExpiry DateTime?

  // Relations
  refreshTokens  RefreshToken[]
  socialAccounts SocialAccount[]
}

model RefreshToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  createdAt DateTime @default(now())
  expiresAt DateTime
  revoked   Boolean  @default(false)

  user   User @relation(fields: [userId], references: [id])
  userId Int
}

model SocialAccount {
  id          Int      @id @default(autoincrement())
  provider    String   // e.g. "twitter", "facebook", "linkedin"
  providerId  String   // user ID from provider
  accessToken String   // short-lived access token
  refreshToken String? // optional (some providers don’t give this)
  expiresAt   DateTime? // when accessToken expires
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user   User @relation(fields: [userId], references: [id])
  userId Int
}